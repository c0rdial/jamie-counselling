---
interface Props {
  class?: string;
  strokeWidth?: number;
}

const { class: className = "w-4 h-4", strokeWidth = 2 } = Astro.props;
---

<svg class={className} fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width={strokeWidth}>
  <path stroke-linecap="round" stroke-linejoin="round" d="M17 8l4 4m0 0l-4 4m4-4H3" />
</svg>
